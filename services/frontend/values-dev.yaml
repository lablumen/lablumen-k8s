# Dev environment overrides for the frontend service.
# image.tag is bumped automatically by service-build-push.yml on every push to main.

image:
  tag: "0a72cf5"
ingress:
  host: app-dev.rnld101.xyz
