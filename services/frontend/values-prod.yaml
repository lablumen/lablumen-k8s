# Prod environment overrides for the frontend service.
# image.tag is bumped automatically by service-release.yml on GitHub Release (semver).

image:
  tag: "2.0.0"
replicaCount: 2
ingress:
  host: app.rnld101.xyz
pdb:
  enabled: true
