# ---- Helm ----
charts/**/charts/        # vendored subchart tgz (none today, but keep ignored)
*.tgz
Chart.lock

# ---- Secrets / env (defense in depth — no secrets belong in this repo) ----
.env
.env.*
!.env.example

# ---- IDE / OS ----
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
.DS_Store
Thumbs.db
